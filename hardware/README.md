# Hardware

Placeholder for Session 12.

Will hold ESP32 firmware and wiring documentation. It replaces the
simulator as the MQTT publisher, using the identical JSON schema, so no
backend or frontend changes are required at that point.
