/**
 * IMPORTANT: "localhost" on your phone means the phone itself, not your
 * PC — so it can never reach a backend running on your computer. Replace
 * this with your PC's actual LAN IP address (see mobile/README.md for how
 * to find it). Your phone and PC must be on the same Wi-Fi network.
 *
 * Example: 'http://192.168.1.42:8000/api/v1'
 */
export const API_BASE_URL = 'http://192.168.1.42:8000/api/v1';
